export const CHANGE_ACTIVE_SECTION_ID = ( state, sectionId ) => {
  	state.activeSectionId = sectionId;
}

export const CHANGE_CLIENT_HEIGHT = ( state, clientHeight ) => {
  	state.clientHeight = clientHeight;
}
